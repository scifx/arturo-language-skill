# Copy to config.env and edit only when command auto-detection is insufficient.
# Values are consumed by bin/ahelp. Keep this file as trusted shell syntax.

ARTURO_BIN=arturo
PYTHON_BIN=python3
AHELP_LIMIT=20
